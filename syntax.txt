
Basic L-System:

rule := sym "->" sym*



Parameterized L-System:
    TODO: Conditional & probabilistic rules

rule := form "->" expr

form := sym | sym "(" var,* ")"

expr := symexpr*
symexpr := sym | sym "(" numexpr,* ")"
numexpr := (var | numlit | arithop)+


cmd := define-cmd | seed-cmd
define-cmd := "#define" var numlit
seed-cmd := "#seed" expr


file := (cmd | rule)*
